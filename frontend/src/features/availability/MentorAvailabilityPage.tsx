import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export function MentorAvailabilityPage() {
  return (
    <PlaceholderPage
      title="Availability"
      description="Configure your recurring weekly availability, session duration, and buffer. Built in a later phase."
    />
  );
}
